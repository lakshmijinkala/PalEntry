# PalNtry Client

Run the client locally:

```bash
cd client
npm install
npm start
```

The development server runs on port 3000 by default and the package.json includes a proxy to http://localhost:5001 so `/api/chat` will call the server.
